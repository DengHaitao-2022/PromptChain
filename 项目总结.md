# PromptChain 项目总结

> 更新时间：2026-05-20
> 文档性质：基于仓库内 Markdown 文档与 `dev@8218f54` 主线事实整理的综合项目总结
> 适用对象：项目指导老师、评审人员、研发成员、后续 MVP2 规划者

---

## 第1章 项目概述

### 1.1 项目背景

PromptChain 是一个面向长文、脚本与结构化内容生产的 AI 工作流平台。项目的核心目标不是“把一次 Prompt 包得更复杂”，而是把复杂内容生成拆分成一条可追踪、可暂停、可恢复、可回放、可人工干预的链式流程。

从仓库现有文档来看，项目的设计动机主要来自单次大模型生成在复杂写作任务中的几个稳定缺陷：

- 长文结构容易漂移，提纲与正文不稳定。
- 当输入不完整时，模型会自行假设，结果不可控。
- 多步骤推理不可见，用户无法在关键节点介入。
- 事实性内容缺乏显式校验，终稿可信度不足。
- 一次生成失败后往往只能整体重来，缺少局部重跑与过程追溯能力。

因此，PromptChain 的总体定位是：

- 以 `Prompt Chain + LangGraph` 为核心的自动化内容生成系统
- 以“多阶段生成 + Human-in-the-Loop + 事实核查 + 全链路追踪”为核心价值
- 以“毕业设计 MVP 可运行、可演示、可回放、可写入论文”为阶段性目标

### 1.2 项目目标

根据 `README.md`、`specs/002-content-gen-mvp1/spec.md` 与 `AGENTS.md`，项目在 MVP1 阶段主要实现五项核心能力：

1. 用户登录与角色权限管理
2. Prompt Chain 工作流可视化编排
3. 一键生成长文或脚本
4. 不确定点检测与 Gate 门控
5. 任务监控与结果回放

在技术上，项目同时追求以下目标：

- 统一运行态契约，避免前后端状态语义漂移
- 中间产物可版本化保存，支持 Trace 与回放
- 工作流定义与工作流运行分离，支持草稿/发布态
- 角色边界明确，支持 viewer / editor / admin / owner 的权限隔离
- 保持系统在棕地增量演进场景下可维护、可扩展

### 1.3 文档整理范围

本总结综合整理了仓库内的以下 Markdown 文档类型：

- 项目说明类：`README.md`、`backend/README.md`、`frontend/README.md`
- 协作与架构事实源：`AGENTS.md`
- MVP1 规格、计划、数据模型、研究、任务、验收文档：
  - `specs/002-content-gen-mvp1/spec.md`
  - `specs/002-content-gen-mvp1/plan.md`
  - `specs/002-content-gen-mvp1/data-model.md`
  - `specs/002-content-gen-mvp1/research.md`
  - `specs/002-content-gen-mvp1/tasks.md`
  - `specs/002-content-gen-mvp1/quickstart.md`
  - `specs/002-content-gen-mvp1/subagent-tasks.md`
  - `specs/002-content-gen-mvp1/promptchain-next-planning-skill.md`
  - `specs/002-content-gen-mvp1/contracts/*.md`
- Provider 升级规格与设计文档：
  - `specs/003-provider-architecture/*.md`
- MVP2 统一错误体系规格与设计文档：
  - `specs/005-unified-error-system/*.md`
- API 文档：
  - `docs/api/*.md`
- 辅助技能清单：
  - `skills_catalog.md`
  - `skills_catalog_zh.md`

### 1.4 文档一致性说明

仓库内文档存在少量时间差异与事实更新差异，整理时遵循以下原则：

1. 以当前主线事实和较新的主线文档为准。
2. 当 `README.md`、旧计划文档与 `AGENTS.md`、`quickstart.md` 冲突时，优先采用 `AGENTS.md` 与 `specs/002-content-gen-mvp1/quickstart.md` 中更新后的主线结论。
3. 当某些历史文档仍以 `dev@c396a48`、`dev@6caaa24` 或更早 commit 作为基线时，本总结会保留其设计意义，但在“当前状态”章节中用 `dev@8218f54` 的主线事实修正。

典型例子包括：

- 运行态默认存储已经从早期内存路径演进为 PostgreSQL-backed store，内存 store 仅作为开发回退。
- 后端 graph 已拆分为 `state.py`、`conditions.py`、`builder.py`、`executor.py`、`runtime_plan.py`，`content_generation_graph.py` 只保留兼容 re-export。
- 首页运行入口、运行记录总览页、详情页重跑和 DOCX 导出已在 `dev@8218f54`，不能继续按早期“占位/未接真实数据”理解。
- 统一错误体系仍在 PR #5，生产 CI/CD 基线仍在 PR #4；二者是候选分支，不是当前 `dev` 已合入事实。

---

## 第2章 需求与业务目标分析

### 2.1 MVP1 业务范围

MVP1 的总体范围可以概括为“围绕内容生成流程的最小可演示闭环”：

- 用户能够注册、登录、验证邮箱并在登录后进入系统
- 设计者能够创建、保存、校验并发布工作流
- 普通用户能够基于已发布工作流发起生成任务
- 系统能够在任务中自动执行意图解析、提纲生成、正文生成、自检与事实核查
- 当出现不确定性或风险时，系统能够触发 Gate 暂停并等待人工决策
- 任务完成后，用户可以查看全过程、产物历史、节点执行记录与回放结果

### 2.2 用户角色与业务角色映射

根据 `specs/002-content-gen-mvp1/contracts/auth-and-access.md` 与 `data-model.md`，系统的技术角色与业务角色映射如下：

| 业务角色 | 技术角色 | 核心权限 |
|---|---|---|
| 普通用户 | `viewer` | 运行已发布工作流、查看本人任务、处理 Gate、查看产物 |
| 工作流设计者 | `editor` | 创建/编辑/校验/发布工作流，并可运行任务 |
| 管理员 | `admin` / `owner` | 成员管理、系统配置、查看全局运行记录、维护工作空间 |

### 2.3 核心用户故事

MVP1 文档定义了五个主要用户故事：

#### 用户故事 1：普通用户发起并完成内容生成任务

用户选择已发布工作流、填写结构化输入后，系统能够自动完成：

- 意图卡生成
- 提纲生成
- 正文/脚本生成
- 自检修订
- 终稿交付

#### 用户故事 2：用户处理 Gate 并继续执行任务

当检测到缺信息、约束冲突或事实风险时，系统能够：

- 暂停任务
- 生成 1~3 个关键问题
- 等待用户回答或审批
- 从中断位置继续执行，而非整体重跑

#### 用户故事 3：工作流设计者创建并发布内容生成流程

设计者可通过可视化编辑器：

- 创建草稿工作流
- 编辑节点和连线
- 进行合法性校验
- 发布可运行版本

#### 用户故事 4：用户或管理员监控并回放任务过程

系统可对运行中或已完成任务提供：

- 当前节点查看
- 状态监控
- 时间线回放
- 节点输入输出查看
- 产物历史查看
- 重跑入口

#### 用户故事 5：管理员维护账号与访问边界

管理员负责：

- 用户与成员管理
- 工作空间边界控制
- 菜单隔离
- 运行态资源访问边界控制

### 2.4 非功能需求

从 `spec.md` 和相关研究文档提炼出的主要非功能要求包括：

- **可用性**：浏览器端即可完成主要流程，无需额外培训
- **可追溯性**：任务的节点运行、产物版本和人工决策必须可回放
- **一致性**：前后端运行态契约保持统一
- **可恢复性**：暂停、等待确认或失败时必须保留上下文
- **权限隔离**：不同角色严格限制功能边界
- **可扩展性**：后续可增加节点类型、模板规则与模型 provider
- **棕地增量演进**：在不推翻现有系统的前提下持续演进

---

## 第3章 技术栈、工程结构与运行环境

### 3.1 技术栈概览

#### 后端

- Python 3.14+
- FastAPI
- LangGraph
- SQLAlchemy / ORM
- PostgreSQL
- Redis
- uv
- Ruff + pre-commit

#### 前端

- Next.js App Router
- React
- TypeScript
- 原生 CSS / 设计 Token 体系
- React Flow（工作流编辑器）

#### LLM / AI 集成

- LangChain
- OpenAI provider
- Anthropic provider
- Ollama provider
- Google Gemini provider（通过 `003-provider-architecture` 升级纳入）

### 3.2 运行环境与本地启动

根据 `README.md`、`backend/README.md` 与 `quickstart.md`，本地环境主要包括三层：

1. **基础设施**
   - PostgreSQL
   - Redis

2. **后端服务**
   - `uv sync`
   - `uv run uvicorn app:app --reload --port 8000`

3. **前端服务**
   - `npm install`
   - `npm run dev`

本地开发中，后端关键环境变量包括：

- `DATABASE_URL`
- `DEFAULT_LLM_PROVIDER`
- `DEFAULT_MODEL_NAME`
- `OPENAI_API_KEY`
- `ANTHROPIC_API_KEY`
- `GEMINI_API_KEY`
- `OLLAMA_BASE_URL`

### 3.3 项目目录结构

从 README、AGENTS 和实际目录结构可归纳出以下主要分层：

```text
PromptChain/
├── backend/
│   ├── app.py
│   ├── core/
│   ├── db/
│   ├── graph/
│   ├── models/
│   ├── nodes/
│   ├── orm/
│   ├── routes/
│   ├── services/
│   └── tests/
├── frontend/
│   └── src/
│       ├── app/
│       ├── components/
│       ├── contexts/
│       └── lib/
├── specs/
│   ├── 002-content-gen-mvp1/
│   └── 003-provider-architecture/
└── AGENTS.md
```

### 3.4 工程协作与文档驱动开发体系

PromptChain 不只是一个业务系统仓库，也包含一套较完整的协作治理体系：

- `AGENTS.md`：项目事实源、架构说明、协作规则
- `specs/002-content-gen-mvp1/*`：MVP1 的规格、研究、计划、任务与验收基线
- `specs/003-provider-architecture/*`：Provider 升级专题的规格与设计
- `skills_catalog*.md`：技能清单，用于驱动 AI 协作工作流
- `promptchain-next-planning-skill.md`：下一轮派工与协作规划模板

这说明项目本身兼具两层属性：

1. 业务系统实现仓库
2. AI 协同开发与 Spec 驱动开发的实验性工程仓库

---

## 第4章 系统总体设计

### 4.1 系统总体架构设计

PromptChain 采用前后端分离单体架构，可概括为“前端应用 + 后端 API + LangGraph 工作流引擎 + 持久化运行态 + 权限与工作空间边界”。

系统总体架构可分为五层：

#### 1. 表现层（Frontend）

由 Next.js App Router 构成，承担以下职责：

- 首页工作流启动
- 工作流详情页与 Gate 交互
- 控制台工作流编辑器
- 成员与权限页面
- 运行记录总览与回放页面

#### 2. 接口层（API Routes）

由 FastAPI 路由层承担，包括：

- 认证接口
- 工作空间接口
- 工作流定义与版本接口
- 内容生成运行态接口
- Trace / Artifact 接口
- WebSocket 实时事件接口

#### 3. 业务服务层（Services）

负责实现核心业务能力，包括：

- 模型 provider 抽象与统一调用入口
- 权限校验
- Artifact Store
- Trace Service
- Rerun Service
- 工作流定义服务

#### 4. 工作流编排层（LangGraph）

LangGraph 承担内容生成链路的状态编排，是业务核心引擎。其职责包括：

- 维护 `GraphState`
- 驱动节点顺序执行
- 处理条件分支与 Gate
- 对接 pause / resume
- 在结束时归档运行态与产物

#### 5. 数据与运行时层（PostgreSQL / Redis）

- PostgreSQL：作为账号、权限、工作流定义、运行态与审计数据的核心存储
- Redis：作为缓存与未来扩展能力的基础设施保留

综合来看，系统不是简单的 CRUD 平台，而是“业务后台 + AI 工作流引擎 + 运行态审计系统”的复合架构。

### 4.2 系统功能结构设计

系统功能可归纳为六大模块：

#### 1. 用户与权限管理模块

- 注册 / 登录 / 登出
- 邮箱验证
- 忘记密码 / 重置密码
- `GET /api/me` 当前身份初始化
- 工作空间与成员关系管理
- RBAC 权限控制

#### 2. 工作流定义与版本管理模块

- 创建工作流草稿
- 可视化节点与连线编辑
- 工作流校验
- 工作流编译预览
- 工作流发布
- 历史版本查看、对比、恢复

#### 3. 内容生成执行模块

- 启动工作流
- 意图卡生成
- 提纲生成
- 正文生成
- 自检修订
- 事实核查
- 最终内容输出

#### 4. Gate 门控与人工干预模块

- 澄清问题 Gate
- 提纲审批 Gate
- 事实核查审批 Gate
- 手动暂停与恢复
- 用户决策写回上下文

#### 5. 追踪、监控与回放模块

- WorkflowRun 进度查看
- NodeRun 节点执行记录查看
- Artifact 历史查看
- Trace 时间线查看
- rerun-options 与 rerun

#### 6. 模型 Provider 与配置模块

- 统一模型访问入口
- Provider registry
- OpenAI / Anthropic / Ollama / Google provider
- 结构化输出路径
- 当前 provider/model 信息读取

### 4.3 工作流模型设计

PromptChain 的工作流模型有两层含义：

#### A. 可视化工作流定义模型

由 `WorkflowDefinition`、`WorkflowVersion`、`WorkflowNode`、`WorkflowEdge` 组成，用于描述流程结构本身。

其特点是：

- 支持草稿与发布态分离
- 支持版本化管理
- 节点类型在持久化层采用通用技术类型：
  - `input`
  - `process`
  - `gate`
  - `checker`
  - `output`
- 业务语义由 `data.config` 和前端模板层表达

#### B. 运行态内容生成工作流模型

由 LangGraph 承载，核心执行链为：

```text
parse_intent
-> (clarify?)
-> generate_outline
-> approve_outline
-> generate_content
-> self_refine
-> check_facts
-> approve_fact_check
-> finalize
```

其设计特点包括：

- 每一步都是一个显式节点
- 每一步输出都可沉淀为 Artifact
- Gate 状态是显式的一等状态，而不是普通暂停
- 支持从指定节点重跑
- 支持运行态与回放分离建模

#### 4.3.1 审批不通过后的明确回退规则

从当前主线代码可以进一步明确两类审批节点的去向：

1. **提纲审批 (`approve_outline`)**
- 用户动作只有三种：`approve`、`modify`、`regenerate`
- `approve`：提纲被确认，并继续流向 `generate_content`
- `modify`：人工修改后的提纲被写成新版本 Artifact，然后继续流向 `generate_content`
- `regenerate`：不会停留在原审批节点，而是通过图的条件路由回到 `generate_outline` 重新生成提纲

因此，提纲审批“不通过”后的明确回退节点是：**`generate_outline`**

2. **事实审批 (`approve_fact_check`)**
- 用户动作只有三种：`confirm`、`use_suggestion`、`manual`
- 当前实现不支持“审批不通过后回到正文生成”或“重新跑事实核查报告”
- 当前实现的真实行为是：
  - `confirm`：保留原结果
  - `use_suggestion`：采用系统建议修订最终内容
  - `manual`：采用人工修订直接修改最终内容
- 审批完成后系统直接流向 `finalize`

因此，事实审批“不通过”后的当前处理方式不是回退到别的节点，而是：**在事实审批节点内完成修订，然后直接进入 `finalize`**

#### 4.3.2 当前尚未完整实现的运行控制能力

虽然配置与字段中已经出现了一些扩展位，但以下能力在当前主线中还没有形成完整、独立、可配置的系统机制：

- Gate 超时未处理后的自动行为
- 多次未通过后的自动终止机制
- 通用重试器
- 熔断器
- 回退策略
- 通用限流器
- 缓存层

当前代码中的 `timeout`、`retry_count` 等更接近：

- 节点配置要求
- 运行态记录字段
- 为后续扩展预留的语义位

而不是已经完全落地的运行控制基础设施。

#### 4.3.3 当前执行方式：异步串行驱动，不是独立事件驱动

从 `backend/graph/executor.py` 可以看出，当前工作流主链的推进方式是：

- 由执行器循环调用 `graph.ainvoke(...)`
- 每个节点内部直接 `await` LLM、数据库与持久化操作
- 节点之间不是通过消息队列或事件总线异步解耦

因此更准确的技术表述应是：

- **当前节点执行是 LangGraph 驱动的异步串行调用**
- **不是严格意义上的事件驱动微服务流程**

### 4.4 数据库设计

从数据模型文档与 ORM 结构可以提炼出数据库设计的四大域。

#### 4.4.1 身份与访问控制域

核心实体包括：

- `User`
- `Workspace`
- `Membership`
- `RefreshToken`
- `EmailVerificationToken`
- `PasswordResetToken`
- `WorkspaceInvite`

这一部分支撑：

- 账号生命周期
- 工作空间隔离
- 成员角色关系
- 会话与 Token 管理

#### 4.4.2 工作流定义与版本域

核心实体包括：

- `WorkflowDefinition`
- `WorkflowVersion`
- `WorkflowNode`
- `WorkflowEdge`

这一域负责：

- 草稿编辑
- 发布版本冻结
- 历史版本对比与恢复

#### 4.4.3 运行态与追踪域

核心实体包括：

- `WorkflowRun`
- `NodeRun`
- `Artifact`
- `GateRecord`（概念实体）
- `ExecutionLog`（概念实体）

这部分是系统最具特色的数据库设计，关键原则是：

- **Write-Once**：产物按版本写入，不覆盖旧结果
- **可回放**：节点输入、输出、异常与人工决策可追溯
- **可恢复**：会话状态需要支持暂停恢复与 Gate 继续执行
- **可重跑**：新的 rerun 创建新的 WorkflowRun，而不是覆盖旧记录

#### 4.4.4 管理与审计域

从 `orm/admin_orm.py` 与文档可看出后台管理域至少包含：

- `ModelProvider`
- `Secret`
- `ApiKey`
- `AuditLog`
- `LoginAttempt`

这一域服务于：

- 模型与密钥配置
- 后台审计
- 登录安全监测

#### 4.4.5 运行态状态模型设计

项目明确区分：

1. **持久化状态**
   - `running`
   - `paused`
   - `completed`
   - `failed`

2. **对外公开状态**
   - `running`
   - `paused`
   - `needs_clarification`
   - `awaiting_outline_approval`
   - `awaiting_fact_check_approval`
   - `completed`
   - `failed`

这一设计的价值是：

- 存储模型简化
- 对外状态语义更完整
- Gate 可读性与 UI 展示更清晰

#### 4.4.6 物理数据表结构清单

下面按当前主线实际 SQLAlchemy ORM 定义，逐张列出数据库表结构。
本节的事实源以以下文件为准：

- `backend/models/auth_orm.py`
- `backend/models/workflow_orm.py`
- `backend/models/admin_orm.py`
- `backend/db/postgres_store.py`

说明：

1. 以下“允许为空”严格按当前 ORM 代码定义读取，而不是按业务理想状态推测。
2. 若某字段未显式声明 `nullable=False`，即使有默认值，也可能在数据库层允许为空。
3. `backend/orm/*` 与 `backend/models/*_orm.py` 存在镜像定义，本节不重复展开，以当前主线实际使用的模型定义为准。
4. 某些字段在业务语义上有关联关系，但 ORM 中未声明数据库级外键，本节会单独备注。

##### 4.4.6.1 身份与访问控制域

##### `users`

- 主键：`id`
- 索引字段：`id`、`email`、`username`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `email` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE / INDEX |
| `username` | `VARCHAR(100)` | 否 | `-` | 是 | UNIQUE / INDEX |
| `display_name` | `VARCHAR(100)` | 否 | `-` | 是 | 无显式索引 |
| `avatar_url` | `VARCHAR(500)` | 否 | `-` | 是 | 无显式索引 |
| `password_hash` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `status` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `email_verified` | `BOOLEAN` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `updated_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `last_login_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### `workspaces`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `name` | `VARCHAR(100)` | 否 | `-` | 否 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `logo_url` | `VARCHAR(500)` | 否 | `-` | 是 | 无显式索引 |
| `owner_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `updated_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### `memberships`

- 主键：`id`
- 索引字段：`id`、`user_id`、`workspace_id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `user_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | INDEX |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `role` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `joined_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `invited_by` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |

备注：`invited_by` 在业务语义上指向邀请人用户 ID，但当前 ORM 未声明数据库级外键。

##### `refresh_tokens`

- 主键：`id`
- 索引字段：`id`、`user_id`、`token_hash`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `user_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | INDEX |
| `token_hash` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE |
| `expires_at` | `DATETIME` | 否 | `-` | 否 | 无显式索引 |
| `revoked_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `user_agent` | `VARCHAR(500)` | 否 | `-` | 是 | 无显式索引 |
| `ip_address` | `VARCHAR(50)` | 否 | `-` | 是 | 无显式索引 |

##### `email_verification_tokens`

- 主键：`id`
- 索引字段：`id`、`user_id`、`token_hash`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `user_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | INDEX |
| `token_hash` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE |
| `expires_at` | `DATETIME` | 否 | `-` | 否 | 无显式索引 |
| `used_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### `password_reset_tokens`

- 主键：`id`
- 索引字段：`id`、`user_id`、`token_hash`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `user_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | INDEX |
| `token_hash` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE |
| `expires_at` | `DATETIME` | 否 | `-` | 否 | 无显式索引 |
| `used_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### `workspace_invites`

- 主键：`id`
- 索引字段：`id`、`workspace_id`、`token_hash`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `email` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `role` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `invited_by` | `VARCHAR(36)` | 否 | `users.id` | 否 | 无显式索引 |
| `token_hash` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE |
| `expires_at` | `DATETIME` | 否 | `-` | 否 | 无显式索引 |
| `accepted_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### 4.4.6.2 工作流定义与版本域

##### `workflow_definitions`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `name` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `version` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `nodes` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `edges` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | 无显式索引 |
| `created_by` | `VARCHAR(36)` | 否 | `users.id` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `updated_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `is_published` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `published_version_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `published_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `published_by` | `VARCHAR(36)` | 否 | `users.id` | 是 | 无显式索引 |
| `is_deleted` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |

备注：`published_version_id` 在业务语义上指向 `workflow_versions.id`，但当前 ORM 未声明数据库级外键。

##### `workflow_versions`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workflow_id` | `VARCHAR(36)` | 否 | `workflow_definitions.id` | 否 | 无显式索引 |
| `version` | `INTEGER` | 否 | `-` | 否 | 无显式索引 |
| `name` | `VARCHAR(255)` | 否 | `-` | 是 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `nodes` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `edges` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `change_log` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `snapshot_type` | `VARCHAR(32)` | 否 | `-` | 是 | 无显式索引 |
| `source_version_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `metadata_json` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `created_by` | `VARCHAR(36)` | 否 | `users.id` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

备注：`source_version_id` 在业务语义上表示来源版本，但当前 ORM 未声明数据库级外键。

##### 4.4.6.3 运行态与追踪域

##### `workflow_runs`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workflow_name` | `VARCHAR(100)` | 否 | `-` | 是 | 无显式索引 |
| `workflow_version` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `workflow_definition_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `workflow_version_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `started_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `completed_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `status` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `current_node` | `VARCHAR(100)` | 否 | `-` | 是 | 无显式索引 |
| `user_input` | `TEXT` | 否 | `-` | 否 | 无显式索引 |
| `final_artifact_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `total_node_runs` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `total_llm_calls` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `total_tokens` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `total_duration_ms` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `metadata_json` | `JSON` | 否 | `-` | 是 | 无显式索引 |

备注：

- `workflow_definition_id`、`workflow_version_id`、`final_artifact_id` 在业务语义上分别关联工作流定义、工作流版本和最终产物，但当前 ORM 未声明数据库级外键。

##### `node_runs`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workflow_run_id` | `VARCHAR(36)` | 否 | `workflow_runs.id` | 否 | 无显式索引 |
| `node_name` | `VARCHAR(100)` | 否 | `-` | 否 | 无显式索引 |
| `node_type` | `VARCHAR(50)` | 否 | `-` | 是 | 无显式索引 |
| `started_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `completed_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `duration_ms` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `status` | `VARCHAR(20)` | 否 | `-` | 是 | 无显式索引 |
| `error_message` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `input_artifact_ids` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `output_artifact_ids` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `llm_calls` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `human_decision` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `retry_count` | `INTEGER` | 否 | `-` | 是 | 无显式索引 |
| `is_rerun` | `BOOLEAN` | 否 | `-` | 是 | 无显式索引 |
| `rerun_from_node_run_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |

备注：`rerun_from_node_run_id` 在业务语义上指向上一次节点运行，但当前 ORM 未声明数据库级外键。

##### `artifacts`

- 主键：`id`
- 索引字段：`id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `type` | `VARCHAR(50)` | 否 | `-` | 否 | 无显式索引 |
| `version` | `INTEGER` | 否 | `-` | 否 | 无显式索引 |
| `content` | `JSON` | 否 | `-` | 否 | 无显式索引 |
| `content_hash` | `VARCHAR(16)` | 否 | `-` | 否 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `parent_version` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `workflow_run_id` | `VARCHAR(36)` | 否 | `workflow_runs.id` | 否 | 无显式索引 |
| `node_run_id` | `VARCHAR(36)` | 否 | `node_runs.id` | 否 | 无显式索引 |
| `metadata_json` | `JSON` | 否 | `-` | 是 | 无显式索引 |

备注：`parent_version` 在业务语义上表示父版本产物 ID，但当前 ORM 未声明数据库级外键。

##### `graph_checkpoints`

- 主键：`thread_id`、`checkpoint_ns`、`checkpoint_id`
- 索引字段：`thread_id`、`checkpoint_ns`、`checkpoint_id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `thread_id` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `checkpoint_ns` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `checkpoint_id` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `parent_checkpoint_id` | `VARCHAR(255)` | 否 | `-` | 是 | 无显式索引 |
| `checkpoint_type` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `checkpoint_payload` | `TEXT` | 否 | `-` | 否 | 无显式索引 |
| `metadata_type` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `metadata_payload` | `TEXT` | 否 | `-` | 否 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### `graph_checkpoint_writes`

- 主键：`thread_id`、`checkpoint_ns`、`checkpoint_id`、`task_id`、`write_index`
- 索引字段：`thread_id`、`checkpoint_ns`、`checkpoint_id`、`task_id`、`write_index`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `thread_id` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `checkpoint_ns` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `checkpoint_id` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `task_id` | `VARCHAR(255)` | 是 | `-` | 否 | PK |
| `write_index` | `INTEGER` | 是 | `-` | 否 | PK |
| `channel` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `task_path` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `value_type` | `VARCHAR(255)` | 否 | `-` | 否 | 无显式索引 |
| `value_payload` | `TEXT` | 否 | `-` | 否 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |

##### 4.4.6.4 管理与审计域

##### `model_providers`

- 主键：`id`
- 索引字段：`id`、`workspace_id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `provider` | `VARCHAR(50)` | 否 | `-` | 否 | 无显式索引 |
| `name` | `VARCHAR(100)` | 否 | `-` | 否 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `enabled` | `BOOLEAN` | 否 | `-` | 是 | 无显式索引 |
| `config` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `updated_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_by` | `VARCHAR(36)` | 否 | `users.id` | 否 | 无显式索引 |

##### `secrets`

- 主键：`id`
- 索引字段：`id`、`workspace_id`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `name` | `VARCHAR(100)` | 否 | `-` | 否 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `ciphertext` | `TEXT` | 否 | `-` | 否 | 无显式索引 |
| `last4` | `VARCHAR(10)` | 否 | `-` | 否 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `updated_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_by` | `VARCHAR(36)` | 否 | `users.id` | 否 | 无显式索引 |

##### `api_keys`

- 主键：`id`
- 索引字段：`id`、`workspace_id`、`key_hash`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `name` | `VARCHAR(100)` | 否 | `-` | 否 | 无显式索引 |
| `description` | `TEXT` | 否 | `-` | 是 | 无显式索引 |
| `key_hash` | `VARCHAR(255)` | 否 | `-` | 否 | UNIQUE |
| `key_prefix` | `VARCHAR(20)` | 否 | `-` | 否 | 无显式索引 |
| `scopes` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `expires_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `revoked_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `last_used_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | 无显式索引 |
| `created_by` | `VARCHAR(36)` | 否 | `users.id` | 否 | 无显式索引 |

##### `audit_logs`

- 主键：`id`
- 索引字段：`id`、`workspace_id`、`user_id`、`action`、`created_at`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `workspace_id` | `VARCHAR(36)` | 否 | `workspaces.id` | 否 | INDEX |
| `user_id` | `VARCHAR(36)` | 否 | `users.id` | 否 | INDEX |
| `action` | `VARCHAR(50)` | 否 | `-` | 否 | INDEX |
| `target_type` | `VARCHAR(50)` | 否 | `-` | 是 | 无显式索引 |
| `target_id` | `VARCHAR(36)` | 否 | `-` | 是 | 无显式索引 |
| `detail` | `JSON` | 否 | `-` | 是 | 无显式索引 |
| `ip_address` | `VARCHAR(50)` | 否 | `-` | 是 | 无显式索引 |
| `user_agent` | `VARCHAR(500)` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | INDEX |

##### `login_attempts`

- 主键：`id`
- 索引字段：`id`、`email`、`ip_address`、`created_at`

| 字段名 | 类型 | 主键 | 外键 | 允许为空 | 索引/约束 |
|---|---|---|---|---|---|
| `id` | `VARCHAR(36)` | 是 | `-` | 否 | PK |
| `email` | `VARCHAR(255)` | 否 | `-` | 否 | INDEX |
| `ip_address` | `VARCHAR(50)` | 否 | `-` | 否 | INDEX |
| `success` | `BOOLEAN` | 否 | `-` | 是 | 无显式索引 |
| `created_at` | `DATETIME` | 否 | `-` | 是 | INDEX |

#### 4.4.7 核心领域类属性清单

除物理数据表外，系统中几个最关键的 Pydantic / 运行态模型属性如下。

##### `User`

定义来源：`backend/models/auth_models.py`

| 属性名 | 含义 |
|---|---|
| `id` | 用户唯一标识 |
| `email` | 登录邮箱 |
| `username` | 用户名 |
| `display_name` | 展示名称 |
| `avatar_url` | 头像地址 |
| `status` | 用户状态 |
| `email_verified` | 邮箱是否验证完成 |
| `created_at` | 创建时间 |
| `updated_at` | 更新时间 |
| `last_login_at` | 最近登录时间 |

##### `Membership`

定义来源：`backend/models/auth_models.py`

| 属性名 | 含义 |
|---|---|
| `id` | 成员关系唯一标识 |
| `user_id` | 用户 ID |
| `workspace_id` | 工作空间 ID |
| `role` | 成员角色 |
| `joined_at` | 加入时间 |
| `invited_by` | 邀请人 ID |

##### `Artifact`

定义来源：`backend/models/artifact.py`

| 属性名 | 含义 |
|---|---|
| `id` | 产物唯一标识 |
| `type` | 产物类型 |
| `version` | 版本号 |
| `content` | 产物正文内容 |
| `content_hash` | 内容哈希 |
| `created_at` | 创建时间 |
| `parent_version` | 父版本产物 ID |
| `workflow_run_id` | 所属工作流运行 ID |
| `node_run_id` | 生成该产物的节点运行 ID |
| `metadata` | 扩展元数据 |

##### `WorkflowRun`

定义来源：`backend/models/artifact.py`

| 属性名 | 含义 |
|---|---|
| `id` | 工作流运行 ID |
| `workflow_name` | 工作流名称 |
| `workflow_version` | 工作流逻辑版本 |
| `workflow_definition_id` | 工作流定义 ID |
| `workflow_version_id` | 工作流发布版本 ID |
| `started_at` | 开始时间 |
| `completed_at` | 结束时间 |
| `status` | 当前运行状态 |
| `current_node` | 当前节点 |
| `user_input` | 用户原始输入 |
| `final_artifact_id` | 最终产物 ID |
| `total_node_runs` | 节点运行总数 |
| `total_llm_calls` | LLM 调用总数 |
| `total_tokens` | token 总数 |
| `total_duration_ms` | 总耗时 |
| `metadata` | 运行态附加元数据 |

##### `NodeRun`

定义来源：`backend/models/artifact.py`

| 属性名 | 含义 |
|---|---|
| `id` | 节点运行 ID |
| `workflow_run_id` | 所属工作流运行 ID |
| `node_name` | 节点名称 |
| `node_type` | 节点类型 |
| `started_at` | 开始时间 |
| `completed_at` | 完成时间 |
| `duration_ms` | 耗时 |
| `status` | 节点状态 |
| `error_message` | 错误信息 |
| `input_artifact_ids` | 输入产物 ID 列表 |
| `output_artifact_ids` | 输出产物 ID 列表 |
| `llm_calls` | LLM 调用记录 |
| `human_decision` | 人工决策记录 |
| `retry_count` | 重试次数 |
| `is_rerun` | 是否由 rerun 产生 |
| `rerun_from_node_run_id` | 来源节点运行 ID |

#### 4.4.8 实际数据库建表语句（基于当前 ORM 生成）

本节给出的 SQL 为依据当前主线 ORM 定义，使用 PostgreSQL 方言生成的真实 `CREATE TABLE` 语句。
需要说明的是：

1. 这些语句反映的是当前代码模型的真实建表口径。
2. `index=True` 生成的独立索引不会全部出现在 `CREATE TABLE` 中，因此索引语句在下一小节单列。
3. 由于当前项目主要通过 SQLAlchemy `Base.metadata.create_all()` 建表，以下 SQL 更适合作为“当前实现的结构说明”与论文附录，而不是手工维护脚本。

##### 4.4.8.1 身份与访问控制域建表语句

```sql
CREATE TABLE users (
	id VARCHAR(36) NOT NULL,
	email VARCHAR(255) NOT NULL,
	username VARCHAR(100),
	display_name VARCHAR(100),
	avatar_url VARCHAR(500),
	password_hash VARCHAR(255) NOT NULL,
	status VARCHAR(20),
	email_verified BOOLEAN,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	updated_at TIMESTAMP WITHOUT TIME ZONE,
	last_login_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id)
);

CREATE TABLE workspaces (
	id VARCHAR(36) NOT NULL,
	name VARCHAR(100) NOT NULL,
	description TEXT,
	logo_url VARCHAR(500),
	owner_id VARCHAR(36) NOT NULL,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	updated_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(owner_id) REFERENCES users (id)
);

CREATE TABLE memberships (
	id VARCHAR(36) NOT NULL,
	user_id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	role VARCHAR(20),
	joined_at TIMESTAMP WITHOUT TIME ZONE,
	invited_by VARCHAR(36),
	PRIMARY KEY (id),
	FOREIGN KEY(user_id) REFERENCES users (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id)
);

CREATE TABLE refresh_tokens (
	id VARCHAR(36) NOT NULL,
	user_id VARCHAR(36) NOT NULL,
	token_hash VARCHAR(255) NOT NULL,
	expires_at TIMESTAMP WITHOUT TIME ZONE NOT NULL,
	revoked_at TIMESTAMP WITHOUT TIME ZONE,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	user_agent VARCHAR(500),
	ip_address VARCHAR(50),
	PRIMARY KEY (id),
	FOREIGN KEY(user_id) REFERENCES users (id),
	UNIQUE (token_hash)
);

CREATE TABLE email_verification_tokens (
	id VARCHAR(36) NOT NULL,
	user_id VARCHAR(36) NOT NULL,
	token_hash VARCHAR(255) NOT NULL,
	expires_at TIMESTAMP WITHOUT TIME ZONE NOT NULL,
	used_at TIMESTAMP WITHOUT TIME ZONE,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(user_id) REFERENCES users (id),
	UNIQUE (token_hash)
);

CREATE TABLE password_reset_tokens (
	id VARCHAR(36) NOT NULL,
	user_id VARCHAR(36) NOT NULL,
	token_hash VARCHAR(255) NOT NULL,
	expires_at TIMESTAMP WITHOUT TIME ZONE NOT NULL,
	used_at TIMESTAMP WITHOUT TIME ZONE,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(user_id) REFERENCES users (id),
	UNIQUE (token_hash)
);

CREATE TABLE workspace_invites (
	id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	email VARCHAR(255) NOT NULL,
	role VARCHAR(20),
	invited_by VARCHAR(36) NOT NULL,
	token_hash VARCHAR(255) NOT NULL,
	expires_at TIMESTAMP WITHOUT TIME ZONE NOT NULL,
	accepted_at TIMESTAMP WITHOUT TIME ZONE,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	FOREIGN KEY(invited_by) REFERENCES users (id),
	UNIQUE (token_hash)
);
```

##### 4.4.8.2 工作流定义与版本域建表语句

```sql
CREATE TABLE workflow_definitions (
	id VARCHAR(36) NOT NULL,
	name VARCHAR(255) NOT NULL,
	description TEXT,
	version INTEGER,
	nodes JSON,
	edges JSON,
	workspace_id VARCHAR(36) NOT NULL,
	created_by VARCHAR(36),
	created_at TIMESTAMP WITHOUT TIME ZONE,
	updated_at TIMESTAMP WITHOUT TIME ZONE,
	is_published INTEGER,
	published_version_id VARCHAR(36),
	published_at TIMESTAMP WITHOUT TIME ZONE,
	published_by VARCHAR(36),
	is_deleted INTEGER,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	FOREIGN KEY(created_by) REFERENCES users (id),
	FOREIGN KEY(published_by) REFERENCES users (id)
);

CREATE TABLE workflow_versions (
	id VARCHAR(36) NOT NULL,
	workflow_id VARCHAR(36) NOT NULL,
	version INTEGER NOT NULL,
	name VARCHAR(255),
	description TEXT,
	nodes JSON,
	edges JSON,
	change_log TEXT,
	snapshot_type VARCHAR(32),
	source_version_id VARCHAR(36),
	metadata_json JSON,
	created_by VARCHAR(36),
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(workflow_id) REFERENCES workflow_definitions (id),
	FOREIGN KEY(created_by) REFERENCES users (id)
);
```

##### 4.4.8.3 运行态与追踪域建表语句

```sql
CREATE TABLE workflow_runs (
	id VARCHAR(36) NOT NULL,
	workflow_name VARCHAR(100),
	workflow_version VARCHAR(20),
	workflow_definition_id VARCHAR(36),
	workflow_version_id VARCHAR(36),
	started_at TIMESTAMP WITHOUT TIME ZONE,
	completed_at TIMESTAMP WITHOUT TIME ZONE,
	status VARCHAR(20),
	current_node VARCHAR(100),
	user_input TEXT NOT NULL,
	final_artifact_id VARCHAR(36),
	total_node_runs INTEGER,
	total_llm_calls INTEGER,
	total_tokens INTEGER,
	total_duration_ms INTEGER,
	metadata_json JSON,
	PRIMARY KEY (id)
);

CREATE TABLE node_runs (
	id VARCHAR(36) NOT NULL,
	workflow_run_id VARCHAR(36) NOT NULL,
	node_name VARCHAR(100) NOT NULL,
	node_type VARCHAR(50),
	started_at TIMESTAMP WITHOUT TIME ZONE,
	completed_at TIMESTAMP WITHOUT TIME ZONE,
	duration_ms INTEGER,
	status VARCHAR(20),
	error_message TEXT,
	input_artifact_ids JSON,
	output_artifact_ids JSON,
	llm_calls JSON,
	human_decision JSON,
	retry_count INTEGER,
	is_rerun BOOLEAN,
	rerun_from_node_run_id VARCHAR(36),
	PRIMARY KEY (id),
	FOREIGN KEY(workflow_run_id) REFERENCES workflow_runs (id)
);

CREATE TABLE artifacts (
	id VARCHAR(36) NOT NULL,
	type VARCHAR(50) NOT NULL,
	version INTEGER NOT NULL,
	content JSON NOT NULL,
	content_hash VARCHAR(16) NOT NULL,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	parent_version VARCHAR(36),
	workflow_run_id VARCHAR(36) NOT NULL,
	node_run_id VARCHAR(36) NOT NULL,
	metadata_json JSON,
	PRIMARY KEY (id),
	FOREIGN KEY(workflow_run_id) REFERENCES workflow_runs (id),
	FOREIGN KEY(node_run_id) REFERENCES node_runs (id)
);

CREATE TABLE graph_checkpoints (
	thread_id VARCHAR(255) NOT NULL,
	checkpoint_ns VARCHAR(255) NOT NULL,
	checkpoint_id VARCHAR(255) NOT NULL,
	parent_checkpoint_id VARCHAR(255),
	checkpoint_type VARCHAR(255) NOT NULL,
	checkpoint_payload TEXT NOT NULL,
	metadata_type VARCHAR(255) NOT NULL,
	metadata_payload TEXT NOT NULL,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (thread_id, checkpoint_ns, checkpoint_id)
);

CREATE TABLE graph_checkpoint_writes (
	thread_id VARCHAR(255) NOT NULL,
	checkpoint_ns VARCHAR(255) NOT NULL,
	checkpoint_id VARCHAR(255) NOT NULL,
	task_id VARCHAR(255) NOT NULL,
	write_index INTEGER NOT NULL,
	channel VARCHAR(255) NOT NULL,
	task_path TEXT,
	value_type VARCHAR(255) NOT NULL,
	value_payload TEXT NOT NULL,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (thread_id, checkpoint_ns, checkpoint_id, task_id, write_index)
);
```

##### 4.4.8.4 管理与审计域建表语句

```sql
CREATE TABLE model_providers (
	id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	provider VARCHAR(50) NOT NULL,
	name VARCHAR(100) NOT NULL,
	description TEXT,
	enabled BOOLEAN,
	config JSON,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	updated_at TIMESTAMP WITHOUT TIME ZONE,
	created_by VARCHAR(36) NOT NULL,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	FOREIGN KEY(created_by) REFERENCES users (id)
);

CREATE TABLE secrets (
	id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	name VARCHAR(100) NOT NULL,
	description TEXT,
	ciphertext TEXT NOT NULL,
	last4 VARCHAR(10) NOT NULL,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	updated_at TIMESTAMP WITHOUT TIME ZONE,
	created_by VARCHAR(36) NOT NULL,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	FOREIGN KEY(created_by) REFERENCES users (id)
);

CREATE TABLE api_keys (
	id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	name VARCHAR(100) NOT NULL,
	description TEXT,
	key_hash VARCHAR(255) NOT NULL,
	key_prefix VARCHAR(20) NOT NULL,
	scopes JSON,
	expires_at TIMESTAMP WITHOUT TIME ZONE,
	revoked_at TIMESTAMP WITHOUT TIME ZONE,
	last_used_at TIMESTAMP WITHOUT TIME ZONE,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	created_by VARCHAR(36) NOT NULL,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	UNIQUE (key_hash),
	FOREIGN KEY(created_by) REFERENCES users (id)
);

CREATE TABLE audit_logs (
	id VARCHAR(36) NOT NULL,
	workspace_id VARCHAR(36) NOT NULL,
	user_id VARCHAR(36) NOT NULL,
	action VARCHAR(50) NOT NULL,
	target_type VARCHAR(50),
	target_id VARCHAR(36),
	detail JSON,
	ip_address VARCHAR(50),
	user_agent VARCHAR(500),
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id),
	FOREIGN KEY(workspace_id) REFERENCES workspaces (id),
	FOREIGN KEY(user_id) REFERENCES users (id)
);

CREATE TABLE login_attempts (
	id VARCHAR(36) NOT NULL,
	email VARCHAR(255) NOT NULL,
	ip_address VARCHAR(50) NOT NULL,
	success BOOLEAN,
	created_at TIMESTAMP WITHOUT TIME ZONE,
	PRIMARY KEY (id)
);
```

#### 4.4.9 实际索引语句（基于当前 ORM 生成）

除主键与唯一约束外，当前代码里通过 `index=True` 显式生成的独立索引如下：

```sql
CREATE UNIQUE INDEX ix_users_username ON users (username);
CREATE UNIQUE INDEX ix_users_email ON users (email);

CREATE INDEX ix_memberships_workspace_id ON memberships (workspace_id);
CREATE INDEX ix_memberships_user_id ON memberships (user_id);

CREATE INDEX ix_refresh_tokens_user_id ON refresh_tokens (user_id);
CREATE INDEX ix_email_verification_tokens_user_id ON email_verification_tokens (user_id);
CREATE INDEX ix_password_reset_tokens_user_id ON password_reset_tokens (user_id);
CREATE INDEX ix_workspace_invites_workspace_id ON workspace_invites (workspace_id);

CREATE INDEX ix_model_providers_workspace_id ON model_providers (workspace_id);
CREATE INDEX ix_secrets_workspace_id ON secrets (workspace_id);
CREATE INDEX ix_api_keys_workspace_id ON api_keys (workspace_id);

CREATE INDEX ix_audit_logs_workspace_id ON audit_logs (workspace_id);
CREATE INDEX ix_audit_logs_created_at ON audit_logs (created_at);
CREATE INDEX ix_audit_logs_user_id ON audit_logs (user_id);
CREATE INDEX ix_audit_logs_action ON audit_logs (action);

CREATE INDEX ix_login_attempts_email ON login_attempts (email);
CREATE INDEX ix_login_attempts_ip_address ON login_attempts (ip_address);
CREATE INDEX ix_login_attempts_created_at ON login_attempts (created_at);
```

### 4.5 接口设计

系统接口可分为四组。

#### 4.5.1 内容工作流接口

核心接口包括：

- `POST /api/workflow/start`
- `GET /api/workflow/{id}`
- `POST /api/workflow/{id}/clarify`
- `POST /api/workflow/{id}/approve-outline`
- `POST /api/workflow/{id}/approve-fact-check`
- `POST /api/workflow/{id}/pause`
- `POST /api/workflow/{id}/resume`
- `GET /api/workflow/{id}/rerun-options`
- `POST /api/workflow/{id}/rerun`
- `GET /api/workflow/{id}/rerun-history`
- `GET /api/workflow/runs`

这些接口统一围绕 `WorkflowResponse` 和运行态读模型设计。

#### 4.5.2 Trace 与 Artifact 接口

- `GET /api/trace/{id}`
- `GET /api/trace/node/{node_run_id}`
- `GET /api/artifact/{artifact_id}`
- `GET /api/artifact/{artifact_id}/history`

其主要目标是：

- 展示任务时间线
- 展示节点详情
- 展示版本化产物

#### 4.5.3 工作流定义与发布接口

- `GET /api/workflows`
- `POST /api/workflows`
- `GET /api/workflows/{workflow_id}`
- `GET /api/workflows/{workflow_id}/definition`
- `PUT /api/workflows/{workflow_id}`
- `PUT /api/workflows/{workflow_id}/definition`
- `POST /api/workflows/{workflow_id}/validate`
- `POST /api/workflows/{workflow_id}/compile`
- `POST /api/workflows/{workflow_id}/publish`
- `GET /api/workflows/{workflow_id}/versions`
- `GET /api/workflows/{workflow_id}/versions/{version_id}`
- `GET /api/workflows/{workflow_id}/versions/compare`
- `POST /api/workflows/{workflow_id}/versions/{version_id}/restore`

#### 4.5.4 认证与权限接口

- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/refresh`
- `POST /api/auth/logout`
- `POST /api/auth/verify-email`
- `POST /api/auth/forgot-password`
- `POST /api/auth/reset-password`
- `GET /api/me`
- 工作空间与成员管理接口组

#### 4.5.5 实时事件接口

基于 WebSocket 的两个通道：

- `/ws/workflow/{workflow_run_id}`
- `/ws/user/{user_id}`

事件类型包括：

- `connected`
- `node_started`
- `node_completed`
- `node_failed`
- `workflow_paused`
- `workflow_resumed`
- `workflow_gate_waiting`
- `workflow_completed`
- `workflow_failed`
- `approval_required`

实时事件的设计原则是：

- WebSocket 负责增强实时体验
- REST 状态与 Trace 时间线仍是权威对账入口

---

## 第5章 系统详细设计与实现

### 5.1 用户登录与权限管理模块实现

该模块由认证路由、认证服务、权限服务、前端认证页面和控制台守卫共同组成。

#### 5.1.1 登录与会话机制

采用 Cookie 会话方案：

- `access_token`：短期访问令牌
- `refresh_token`：刷新令牌

关键特征：

- HttpOnly Cookie 传输
- 前端受保护请求默认 `credentials: include`
- 通过 `GET /api/me` 获取当前用户、工作空间、当前角色

#### 5.1.2 注册与邮箱验证闭环

系统已实现：

- 注册
- 邮箱验证
- 登录
- 忘记密码
- 重置密码

完整链路为：

```text
register -> verify-email -> login
forgot-password -> reset-password -> login
```

这意味着当前登录闭环有两个明确结论：

- **短信验证码不存在**
- **邮箱验证是新注册用户进入正常登录态的必经步骤**

也就是说，系统当前采用的是“邮箱验证激活账号”路径，而不是“短信验证码登录”或“注册后可直接登录”路径。

#### 5.1.3 RBAC 权限实现

权限矩阵由后端 `permission_service` 与前端 `auth.ts` 镜像维护。

关键实现点：

- `viewer` 具备查看与执行已发布工作流能力
- `editor` 负责工作流编辑、发布
- `admin/owner` 可跨用户查看同工作空间运行态资源
- Trace、Artifact、Rerun 等运行态资源遵循相同归属校验

#### 5.1.4 工作空间与成员管理

系统在账号体系之上增加工作空间边界：

- 用户可拥有多个工作空间关系
- 资源默认按工作空间隔离
- 成员可被邀请、修改角色、移除

这使系统具备面向团队使用的最小组织边界。

### 5.2 工作流编排模块实现

工作流编排模块分为“定义侧”和“运行侧”两部分。

#### 5.2.1 定义侧：工作流草稿编辑

定义侧主要由工作流编辑器承担，支持：

- 新建工作流
- 编辑节点和连线
- 保存草稿
- 恢复草稿
- 校验流程合法性

#### 5.2.2 版本与发布机制

设计者在工作流完成后可执行：

- `validate`
- `compile`
- `publish`

发布机制的核心是：

- 草稿保存不等于发布
- 发布后生成稳定版本快照
- 普通用户只能运行发布版本
- 历史版本支持 compare / restore

结合前后端当前实现，可以进一步明确：

1. **校验或编译失败后返回哪个界面**
- 当前前端编辑页的主交互围绕：
  - 保存草稿
  - `validateWorkflow(..., 'publish')`
  - `publishWorkflow(...)`
- 当校验或发布前检查失败时，用户**不会离开当前工作流编辑页**
- 错误信息通过同页的 `statusMessage`、`validation` 和 `actionError` 区域反馈

2. **compile 能力的真实位置**
- 后端存在 `POST /api/workflows/{workflow_id}/compile` 接口
- 后端服务也存在 `WorkflowDefinitionService.compile()`
- 但当前前端并没有把 compile 做成一个单独的“跳转到编译结果页”的完整流程

3. **恢复版本后是否自动发布**
- 不会自动发布
- `restore()` 的真实行为是：把历史版本恢复成一个新的草稿版本
- 用户仍需回到编辑器，由人工重新执行发布动作

#### 5.2.3 节点模型设计

节点持久化层采用通用类型，不把业务语义直接固化为数据库枚举。这一设计降低了演进成本：

- 技术类型稳定
- 业务模板可继续扩展
- 编辑器与后端不必频繁做枚举迁移

### 5.3 内容生成主链实现

这是 PromptChain 的核心实现。

#### 5.3.1 链路步骤

内容生成主链的 canonical 流程是：

1. `parse_intent`
2. `clarify`（必要时）
3. `generate_outline`
4. `approve_outline`
5. `generate_content`
6. `self_refine`
7. `check_facts`
8. `approve_fact_check`
9. `finalize`

#### 5.3.2 GraphState 设计

GraphState 用于承载整个运行过程中的上下文信息，典型字段包括：

- 用户输入
- 意图卡
- 澄清问题与回答
- 提纲与审批结果
- 正文与修订结果
- 事实核查报告与人工决策
- 错误信息

这使系统能够：

- 在节点之间传递结构化状态
- 在 Gate 暂停后继续执行
- 在回放中展示完整过程

#### 5.3.3 节点产物版本化实现

内容生成的每个阶段都会生成可回看的中间产物：

- Intent Card
- Outline
- Section Content
- Fact Check Report
- Final Content

这些产物写入 `Artifact`，并通过 `NodeRun` 记录其来源。

#### 5.3.4 运行态持久化

根据研究与 AGENTS 中的更新事实，运行态默认已经从纯内存方案转向 PostgreSQL 作为权威实现，内存 Store 仅作为开发回退路径。

这意味着系统已经具备：

- 运行态持久化
- pause / resume 所需上下文保留
- Trace / Replay 所需数据基础

#### 5.3.5 Artifact、NodeRun、WorkflowRun 的典型写入顺序

从 `intent_parser.py`、`content_generator.py`、`self_refiner.py`、`fact_checker.py` 等节点实现可以归纳出一个稳定顺序：

1. 创建 `NodeRun`
2. 执行节点逻辑（LLM 调用、人工决策或数据处理）
3. 写入一个或多个 `Artifact`
4. 将 Artifact ID 追加到 `NodeRun.output_artifact_ids`
5. 更新并完成 `NodeRun`
6. 由执行器同步 `WorkflowRun` 的 `current_node`、`status` 和 `metadata`

因此：

- **Trace 并不是单独先写库、再回头写 Artifact**
- **Artifact 通常先于 NodeRun 完成写入**
- **WorkflowRun 的最终状态同步发生在节点执行结果之后**

### 5.4 Gate 门控模块实现

Gate 模块是 PromptChain 区别于普通内容生成工具的关键。

#### 5.4.1 Gate 类型

系统当前显式支持三类 Gate：

1. `needs_clarification`
2. `awaiting_outline_approval`
3. `awaiting_fact_check_approval`

#### 5.4.2 Gate 与普通暂停的区别

文档明确区分：

- `paused`：用户主动暂停
- `needs_clarification` / `awaiting_*`：系统进入 Gate 等待

这种设计的优点是：

- 前端显示更清晰
- API 语义更稳定
- 回放与审计能区分“为什么停下来”

#### 5.4.3 Gate 记录内容

虽然当前未必以单独 ORM 落地，但逻辑上 GateRecord 需要表达：

- Gate 类型
- 触发原因
- 关键问题
- 用户答案/审批决策
- 打开时间与处理时间
- 最终解决方式

#### 5.4.4 Gate 继续执行

用户通过对应接口提交回答后：

- 回答写回 GraphState
- 系统从中断位置继续执行
- 不重新创建任务实例

这满足了“链路可继续而非整体重跑”的设计目标。

#### 5.4.5 Gate 节点的明确后续行为

从当前主线实现可以进一步精确描述：

1. **澄清 Gate**
- 用户回答澄清问题后，系统把回答写回 `GraphState`
- 然后从中断点继续执行后续节点

2. **提纲审批 Gate**
- `approve`：继续进入正文生成
- `modify`：写入新的提纲 Artifact，再进入正文生成
- `regenerate`：回到 `generate_outline`

3. **事实核查审批 Gate**
- `confirm`：保留当前内容并继续
- `use_suggestion`：应用系统建议修正，再继续
- `manual`：应用人工修正，再继续

当前实现中，事实审批节点**不会**：

- 重新生成事实核查报告
- 回退到正文生成重新写正文
- 自动进入新的审批轮次

#### 5.4.6 当前未定义或未实现的 Gate 系统行为

需要明确指出，以下行为目前没有形成主线中的清晰机制：

- 超时未处理时系统自动如何反应
- 多次未通过后系统是否自动终止
- 审批失败次数上限
- Gate 超时升级到管理员或系统任务

因此这些点在当前文档中应表述为：

- **当前未定义**
- 或 **当前未实现**

而不是默认视为已具备。

### 5.5 任务监控与结果回放实现

#### 5.5.1 WorkflowRun 监控

系统可查看：

- 任务当前状态
- 当前节点
- 历史节点
- 最近 Gate 状态
- 最近 pause 状态

#### 5.5.2 Trace 时间线

`GET /api/trace/{id}` 是回放的权威入口，时间线可展示：

- 节点开始/结束
- 节点失败
- workflow_paused
- workflow_resumed
- workflow_gate_waiting

当实时事件不完整时，Trace 时间线还能做补偿，这一点对回放一致性非常关键。

#### 5.5.3 Trace Service 与数据库写入职责

当前 `TraceService` 的职责主要是：

- 读取 `WorkflowRun`
- 读取 `NodeRun`
- 读取 `Artifact`
- 把这些记录重新组合为时间线、节点详情和版本历史

因此更准确的内部实现描述是：

- **TraceService 主要负责读和回放，不是独立的数据库写入协调器**
- 实际写库动作主要发生在：
  - 各节点函数内部
  - `artifact_store / postgres_store`
  - `executor` 对 `WorkflowRun` 的状态同步

#### 5.5.4 Trace 与 Artifact 的写入先后顺序

当前系统并不存在一个名为“Trace”的独立持久化表。

所谓 Trace 时间线，实际上是由以下已有记录组合出来的：

- `NodeRun.started_at / completed_at / status`
- `LLMCallRecord`
- `HumanDecision`
- `Artifact.created_at`

因此可以明确：

- **Artifact 先写入数据库**
- **NodeRun 再记录其输入/输出 Artifact ID 并完成状态更新**
- **Trace 时间线是读取时计算生成，而不是独立先写库**

#### 5.5.5 Artifact 历史与版本追踪

通过：

- `GET /api/artifact/{artifact_id}`
- `GET /api/artifact/{artifact_id}/history`

用户可查看单个产物及其版本历史。这也是论文答辩或系统演示时非常重要的“证据链”能力。

#### 5.5.6 WebSocket 与 Trace 的关系

WebSocket 当前明确发送的是关键运行态事件，包括：

- `node_started`
- `node_completed`
- `node_failed`
- `workflow_paused`
- `workflow_resumed`
- `workflow_gate_waiting`
- `workflow_completed`
- `workflow_failed`
- `approval_required`

但需要强调：

- **不是每一步内部写库动作都会发送 WebSocket**
- **不是每个 Artifact 创建都会单独发送事件**
- WebSocket 用于增强实时感知
- REST + Trace 仍是最终的权威对账入口

#### 5.5.7 rerun 机制

系统支持：

- 查询 rerun options
- 从指定节点创建新的 rerun 任务
- 查看 rerun history

该能力本质上体现了系统的“局部恢复”与“局部再执行”能力，而非简单重启整个任务。

### 5.6 Provider 与模型访问层实现

虽然这不是你点名要求的 5 个子模块之一，但从现有 Markdown 文档来看，它已经成为系统可用性的重要组成部分。

#### 5.6.1 统一入口

系统保持三个统一入口不变：

- `get_llm()`
- `get_structured_llm()`
- `get_current_model_info()`

#### 5.6.2 Provider registry

Provider 层通过静态 registry 管理支持列表，目前的目标支持集合为：

- `openai`
- `anthropic`
- `ollama`
- `google`

#### 5.6.3 Google Provider 升级

`003-provider-architecture` 文档说明，这次升级的目标是：

- 新增 Google Gemini 支持
- 显式消费 `GEMINI_API_KEY`
- 保持结构化输出路径不变
- 保持调用面不变
- 不演进为复杂插件系统

这说明模型访问层已经从“只支持三类 Provider”演进为“具备四类 Provider 的统一抽象基线”。

---

## 第6章 当前实现状态与阶段结论

### 6.1 当前主线已实现内容

综合 `AGENTS.md`、`quickstart.md`、PR 演进和相关文档，`dev@8218f54` 当前主线已基本具备以下能力：

1. 认证闭环已实现
2. RBAC 与工作空间边界已实现
3. 内容生成主链路已实现
4. Gate 机制已实现
5. Trace / Artifact / Rerun 已实现
6. 工作流定义、发布、版本对比与恢复已实现
7. 共享前端 runtime client 与 runs overview 已并入主线
8. Google provider 支持已并入主线
9. 工作流详情页完成态阅读、SSE 快照流、节点重跑历史与 DOCX 导出已并入主线
10. 已发布可视化工作流定义可编译为当前内容生成引擎可执行的受限 `runtime_plan`

### 6.2 MVP1 当前剩余工作

在文档层面，MVP1 已接近尾声，当前剩余工作主要是“验收型”而不是“核心功能型”。

#### 1. 验收闭环归档

- `T017`
- `T024`
- `T031`
- `T037`
- `T042`

主要是：

- US1 / US2 / US3 / US4 / US5 的 smoke / quickstart / access 验证归档
- 把“代码已实现”沉淀为“可复用的验收基线”
- 在 `dev@8218f54` 上重新执行首页启动、Gate、pause/resume、工作流发布可见性、运行记录、Trace、Artifact history、Rerun、SSE、DOCX 导出和 auth/access smoke

#### 2. PR 候选收口

- PR #5：统一错误体系与前端错误归一化。当前 CodeQL 通过且 mergeable，但尚未合入 `dev`，因此不能写成主线 API 契约。
- PR #4：生产 CI/CD 与部署基线。当前 CodeQL 通过，但 PR CI 的后端质量检查和前端质量检查失败，因此不能作为生产部署完成项归档。

### 6.3 从文档角度对 MVP1 的总体判断

如果从“系统功能是否基本闭环”的角度看，MVP1 已进入尾声。

原因是：

- 核心链路已具备
- 主线功能大块已进 `dev`
- 后续剩余事项主要是 live smoke、验收归档和 PR gate 收口

因此，从项目管理角度，当前阶段非常适合：

- 一边完成 MVP1 的验收归档
- 一边启动 MVP2 的需求规划和范围定义

### 6.4 文档中反映出的系统优势

1. **链路清晰**：从用户输入到终稿输出路径明确
2. **状态显式**：Gate 与 pause 区分清楚
3. **可追踪性强**：NodeRun / Artifact / WorkflowRun 三层追踪完整
4. **可恢复性强**：支持暂停、恢复、Gate 继续执行和 rerun
5. **权限边界清晰**：viewer / editor / admin / owner 角色语义完整
6. **扩展性较好**：工作流定义、节点模板和 provider 可继续演进
7. **交付形态更完整**：详情页已经具备完成态阅读、Markdown 下载和 DOCX 导出能力

### 6.5 文档中反映出的风险与不足

1. MVP1 仍缺 `dev@8218f54` 上的最终 live smoke，因此不能直接写成“验收完成”
2. WebSocket / SSE 虽已接通，但仍是实时体验增强通道，不是唯一事实源
3. 统一错误体系尚未合入主线，当前 `dev` 仍保留多套错误风格
4. 生产 CI/CD 与部署基线仍在候选 PR，且存在失败 checks
5. 生产级能力仍不足：版本化数据库迁移、独立 worker/queue、通用重试/超时/熔断、完整可观测性和灾难恢复尚未闭环
6. Provider 升级目前仍以最小基线为主，后续还可继续完善错误语义、限流、回退、成本统计和更多 provider 治理能力

---

## 第7章 结论与后续建议

### 7.1 项目总体结论

PromptChain 已经从一个“单纯的 AI 内容生成原型”演进为一个具备以下特征的系统：

- 有登录与角色边界
- 有工作流定义与发布机制
- 有多阶段内容生成主链
- 有人工门控能力
- 有任务监控、追踪、回放与重跑能力
- 有统一模型访问层与多 provider 兼容能力
- 有完成态阅读、Markdown 下载和 DOCX 导出的交付闭环

从课程设计/毕业设计视角看，它已经具备比较完整的“系统设计 + 核心实现 + 验收路径 + 演进空间”。

### 7.2 对 MVP2 的建议方向

如果进入 MVP2，建议重点考虑以下方向：

1. **MVP1 最终验收归档**
   - 先在 `dev@8218f54` 上跑通 quickstart 的 Smoke A-G，形成最终 sign-off 证据

2. **统一错误体系**
   - 推进 PR #5 的 review / CI / merge gate
   - 合入后继续收口 `auth.ts`、设置页、编辑器 hook 等旧错误解析路径

3. **生产 CI/CD 与部署**
   - 修复 PR #4 的后端质量检查和前端质量检查失败
   - 再把生产 compose、Dockerfile、部署说明纳入可复用发布基线

4. **数据库迁移与运行可靠性**
   - 引入 Alembic 等版本化迁移体系
   - 将进程内后台任务演进为 worker / queue 模式
   - 补齐超时、重试、熔断、限流、死信和恢复策略

5. **模型治理能力增强**
   - 完善 provider 错误语义
   - 考虑模型配置治理、失败回退与更强的 provider 可观测性

6. **内容质量控制增强**
   - 强化事实核查、风险策略、内容评估与质量指标

### 7.3 对文档体系的建议

后续建议把文档分成三层维护：

1. **对外项目说明**
   - `README.md`

2. **主线事实源**
   - `AGENTS.md`
   - `specs/002-content-gen-mvp1/quickstart.md`

3. **专题设计文档**
   - `specs/003-provider-architecture/*`
   - `specs/005-unified-error-system/*`
   - 后续 MVP2 spec / plan / tasks / contracts

这样能够减少“README 过时、spec 与主线不同步”的问题。

---

## 附录A：本总结的主要文档来源

### A.1 项目说明与环境文档

- `README.md`
- `backend/README.md`
- `frontend/README.md`

### A.2 主线事实与协作文档

- `AGENTS.md`
- `specs/002-content-gen-mvp1/subagent-tasks.md`
- `specs/002-content-gen-mvp1/promptchain-next-planning-skill.md`

### A.3 MVP1 设计与验收文档

- `specs/002-content-gen-mvp1/spec.md`
- `specs/002-content-gen-mvp1/plan.md`
- `specs/002-content-gen-mvp1/data-model.md`
- `specs/002-content-gen-mvp1/research.md`
- `specs/002-content-gen-mvp1/tasks.md`
- `specs/002-content-gen-mvp1/quickstart.md`
- `specs/002-content-gen-mvp1/checklists/requirements.md`
- `specs/002-content-gen-mvp1/contracts/runtime-api.md`
- `specs/002-content-gen-mvp1/contracts/realtime-events.md`
- `specs/002-content-gen-mvp1/contracts/workflow-definition-api.md`
- `specs/002-content-gen-mvp1/contracts/auth-and-access.md`

### A.4 Provider 升级专题文档

- `specs/003-provider-architecture/spec.md`
- `specs/003-provider-architecture/plan.md`
- `specs/003-provider-architecture/data-model.md`
- `specs/003-provider-architecture/research.md`
- `specs/003-provider-architecture/tasks.md`
- `specs/003-provider-architecture/quickstart.md`
- `specs/003-provider-architecture/checklists/requirements.md`
- `specs/003-provider-architecture/contracts/provider-config.md`
- `specs/003-provider-architecture/contracts/provider-service.md`

### A.5 MVP2 统一错误体系专题文档

- `specs/005-unified-error-system/spec.md`
- `specs/005-unified-error-system/plan.md`
- `specs/005-unified-error-system/data-model.md`
- `specs/005-unified-error-system/research.md`
- `specs/005-unified-error-system/tasks.md`
- `specs/005-unified-error-system/quickstart.md`
- `specs/005-unified-error-system/contracts/error-envelope.md`
- `specs/005-unified-error-system/contracts/error-code-registry.md`

### A.6 API 文档

- `docs/api/index.md`
- `docs/api/workflow.md`
- `docs/api/monitoring.md`
- `docs/api/auth.md`
- `docs/api/workspace.md`
- `docs/api/workflow-definition.md`
- `docs/api/openapi.v1.yaml`
- `docs/api/openapi.v1.json`

### A.7 辅助目录与技能清单

- `skills_catalog.md`
- `skills_catalog_zh.md`

这些文档不直接定义业务系统功能，但反映了 PromptChain 仓库在 AI 协同开发、技能发现和流程治理方面的辅助体系。
